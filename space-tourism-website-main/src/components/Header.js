export default const Header = () => {
    return (
        <picture>
            <img src={logo} alt="logo" />
        </picture>
        <nav>
            <ul>
                <li><a>00</a></li>
            </ul>
        </nav>
    )
}